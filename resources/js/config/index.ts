/**
 * @description group all app settings
 */
import setting from "./setting.config";
import theme from "./theme.config";
// import network from "./net.config";
export default { ...setting, ...theme };
